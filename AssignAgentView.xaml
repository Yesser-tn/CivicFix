<Window x:Class="CivicFix.UI.AssignAgentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Assign Agent"
        Height="300"
        Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A"
        ResizeMode="NoResize">

    <Grid>
        <Border Background="#020617"
                CornerRadius="16"
                Padding="16"
                Width="340"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                BorderBrush="#1E293B"
                BorderThickness="1">

            <StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,14">

                    <TextBlock FontFamily="Segoe Fluent Icons"
                               Text="&#xE77B;"
                               FontSize="16"
                               Foreground="#60A5FA"
                               Margin="0,0,8,0"/>

                    <TextBlock Text="Assign Agent"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="#E5E7EB"/>
                </StackPanel>

                <TextBlock Text="Select available agent"
                           FontSize="13"
                           Foreground="#94A3B8"
                           Margin="0,0,0,6"/>

                <ComboBox ItemsSource="{Binding AvailableAgents}"
          DisplayMemberPath="Name"
          SelectedValuePath="Id"
          SelectedValue="{Binding SelectedAgentId}"
          Height="34"
          Padding="8,3"
          FontSize="13"
          Background="White"
          Foreground="Black"
          BorderBrush="#334155">

                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                            <Style.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>

                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.ItemContainerStyle>

                </ComboBox>

                <Button Height="36"
                        Margin="0,18,0,0"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="Assign_Click">

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe Fluent Icons"
                                   Text="&#xE8FD;"
                                   FontSize="14"
                                   Margin="0,0,6,0"/>
                        <TextBlock Text="Assign Agent"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>
