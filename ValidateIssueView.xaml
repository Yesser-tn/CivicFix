<Window x:Class="CivicFix.UI.ValidateIssueView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Validate Issue"
        Height="240"
        Width="340"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A"
        ResizeMode="NoResize">

    <Grid>
        <Border Background="#020617"
                CornerRadius="16"
                Padding="16"
                Width="300"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                BorderBrush="#1E293B"
                BorderThickness="1">

            <StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,14">
                    <TextBlock FontFamily="Segoe Fluent Icons"
                               Text="&#xE8FD;"
                               FontSize="18"
                               Foreground="#60A5FA"
                               Margin="0,0,8,0"/>
                    <TextBlock Text="Validation Action"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#E5E7EB"/>
                </StackPanel>

                <TextBlock Text="Select action"
                           FontSize="12"
                           Foreground="#94A3B8"/>

                <ComboBox x:Name="ActionBox"
          Height="34"
          Padding="8,3"
          FontSize="13"
          Background="White"
          Foreground="Black"
          BorderBrush="#334155"
          Margin="0,4,0,14">

                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Padding" Value="8,4"/>

                            <Style.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.ItemContainerStyle>

                    <ComboBoxItem Content="Validated"/>
                    <ComboBoxItem Content="Rejected"/>
                </ComboBox>

                <Button Height="36"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="Confirm_Click">

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe Fluent Icons"
                                   Text="&#xE73E;"
                                   FontSize="14"
                                   Margin="0,0,6,0"/>
                        <TextBlock Text="Confirm"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>
