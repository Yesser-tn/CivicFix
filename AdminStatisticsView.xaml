<Window x:Class="CivicFix.UI.AdminStatisticsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Statistics"
        Height="300"
        Width="400"
        WindowStartupLocation="CenterScreen">

    <StackPanel Margin="20">

        <TextBlock Text="System Statistics"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Text="{Binding TotalIssues, StringFormat=Total Issues: {0}}"/>
        <TextBlock Text="{Binding CreatedIssues, StringFormat=Created: {0}}"/>
        <TextBlock Text="{Binding InProgressIssues, StringFormat=In Progress: {0}}"/>
        <TextBlock Text="{Binding ResolvedIssues, StringFormat=Resolved: {0}}"/>

    </StackPanel>
</Window>
